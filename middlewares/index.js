
const GlobalConfigs = require('./handlers/globalConfigs');
const NotFound = require('./handlers/notFound');
const InternalServerError = require('./handlers/internalServerError');

module.exports = {
  GlobalConfigs,
  NotFound,
  InternalServerError
}
